import styled from 'styled-components'

export const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  max-height: 34rem;
  max-width: 38.5rem;

  &:hover {
    transform: scale(1.1);
  }
`
